def loss(state, xmeas, xmv):
    # cost = 0
    # if (tripped()):
    #     cost += 20_000
    #     sleep(3600e3)
    # prod_rate = xmeas[17] * (612. * 0.01 * xmeas[40] + 617. * 0.01 * xmeas[41]) 
    # cost += (xmeas[42] - 1)**2 * prod_rate 
    return 0.0
